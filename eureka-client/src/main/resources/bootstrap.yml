spring:
  cloud:
    config:
      name: eureka-client
      label: master
      discovery:
        enabled: true
        serviceId: config
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8001/eureka/